{
  "name": "mdssc-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for the MDSSC CI scripts (Track C) — runs ci/scripts/*.sh against a scripted mock MDSSC server.",
  "scripts": {
    "test": "playwright test",
    "mock-server": "node mock-server/server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "express": "^4.21.0",
    "multer": "^2.0.1"
  }
}
